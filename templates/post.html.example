<!DOCTYPE html>
<html>
	<head>	
		<title>nickpegg.com</title>
		
		<link rel="stylesheet" href="main.css" type="text/css" />
	</head>
	
	<body>
		<header>
			<a class="header" href=""><h1>nickpegg.com</h1></a>
		</header>

		<nav id="menu" class="menu">
			<h2 class="menu">Menu</h2>
			{% for p in pages %}
				{% if p.parent == None %}
					<a class="item" href="{{p.href}}">{{p.title}}</a>
				{% else %}
					<a class="subitem" href="{{p.href}}">{{p.title}}</a>
				{% endif %}
			{% endfor %}
		</nav>
		
		<div id="content">
			<article class="post">
				{% for p in posts %}
					<a class="postitle" }href="{{a.href}}">{{a.title}}</a>
					<span class="info">Posted on {{a.date}}</span>
					{{a.body | markdown}}					
				{% endfor %}
			</article>
		</div>

		<footer class="info">
			<p>Copyright 2010 Nick Pegg</p>
			<p>Powered by <a href="http://nickpegg.com/pages/posty.html">Posty</a></p>
		</footer>
	</body>
</html>